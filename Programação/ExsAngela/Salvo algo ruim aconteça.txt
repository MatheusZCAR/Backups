void ler_vetor (int *N){

int * vet;

printf("\n Numero de elementos do vetor:");
scanf("%d", N);

///vet = (int*) calloc (*n, sizeof(int)); tentar caso nn funcione

if((Pvet = (float*)calloc(N, sizeof(float))) == NULL)exit (1);
for (int i=0; i<N; i++) scanf("%f", &Pvet[i]);

return Pvet
